#include <stdio.h>
#include <stdlib.h>

// Define global constant of capacity for hashmap
#define CAPACITY 5000


int main()
{
  int numIdenticalPairs(int* nums, int numsSize);
  int nums[] = {1,2,3,1,1,3};
  int numsSize = sizeof(nums) / sizeof(int);

  return 0;
}

  int numIdenticalPairs(int* nums, int numsSize)
  {
    int ans = 0;
    
    for(int i = 0; i < numsSize; i ++)
    {
      ;
    }
    return ans;
  }
